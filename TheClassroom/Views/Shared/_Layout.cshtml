<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="author" content="Tomáš Vondra">
    <link rel="shortcut icon" type="image/png" href="@Url.Content("~/favicon.png")" />
    <title>The Classroom</title>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link rel="stylesheet" href="@Url.Content("~/Content/Home.css")" />
    <link rel="stylesheet" href="@Url.Content("~/Content/Login.css")" />

</head>
<body>
           @RenderBody()
   

   @* <script>
        $(document).ready(function () {
            $(".About").click(function () {
                if ($(".oProjektu").is(":hidden")) {
                    $(".oProjektu").slideDown("slow", function () {
                        $(".oProjektu").children().fadeIn("slow");
                    });
                }
                else {
                    $(".oProjektu").children().fadeOut("slow", function () {
                        $(".oProjektu").slideUp("slow");
                    });
                }
            });
        });
    </script> *@
</body>
</html>
